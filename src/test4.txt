# julia code for Raspberry Pi

julia> using Pkg

julia> Pkg.activate(".")

julia> using AppliInvoicing

julia> using Sockets

julia> using Serialization

julia> io = connect("192.168.2.11", 8000);

julia> serialize(io, "START")

julia> stms = AppliInvoicing.read_bank_statements("./bank.csv");

julia> serialize(io, stms)
